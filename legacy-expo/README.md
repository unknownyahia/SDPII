# Legacy Expo PoC

This folder contains the archived Expo proof-of-concept client that originally lived at the repo root.

Status:

- archived for reference only
- not the active mobile app
- not the recommended runtime path

The active implementation is the React Native CLI app in:

- `../mobile/`

If you inspect this folder, treat it as historical reference rather than the current source of truth.
